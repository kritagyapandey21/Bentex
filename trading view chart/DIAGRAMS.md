# Visual Reference Guide

Quick visual diagrams and flowcharts for understanding the system.

## System Overview Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER'S BROWSER                           │
│                                                                   │
│  📱 Demo.html (Lightweight Charts)                               │
│  📱 TradingView Demo.html (Datafeed)                             │
│                                                                   │
│  Uses identical RNG/Generator code as server ✓                   │
└───────────────────┬─────────────────────┬───────────────────────┘
                    │                     │
                HTTP REST             WebSocket
                    │                     │
┌───────────────────▼─────────────────────▼───────────────────────┐
│                    🖥️  NODE.JS SERVER                            │
│                                                                   │
│  Express.js API         WebSocket Server                         │
│  ├─ GET /api/ohlc       ├─ Connection mgmt                       │
│  ├─ POST /save_candle   └─ Broadcast updates                     │
│  └─ GET /last_saved                                              │
│                                                                   │
│  Deterministic Generator (shared/generator.js)                   │
│  └─ Same code as client for parity ✓                             │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
                    ┌───────────────────────┐
                    │  💾 SQLite Database   │
                    │                       │
                    │  UNIQUE constraint    │
                    │  prevents duplicates  │
                    └───────────────────────┘
```

## Data Flow: Initial Chart Load

```
     Browser                Server              Database
        │                      │                    │
        │   1. GET /api/ohlc   │                    │
        ├─────────────────────>│                    │
        │                      │  2. Query candles  │
        │                      ├───────────────────>│
        │                      │<───────────────────┤
        │                      │  3. Return rows    │
        │                      │                    │
        │                      │  4. Generate       │
        │                      │     partial        │
        │                      │     candle         │
        │  5. Return JSON      │                    │
        │<─────────────────────┤                    │
        │                      │                    │
        │  6. Render chart     │                    │
        │     with candles     │                    │
        │                      │                    │
        │  7. Subscribe WS     │                    │
        ├─────────────────────>│                    │
        │<─────────────────────┤                    │
        │  8. Connected        │                    │
        │                      │                    │
```

## Data Flow: Save & Broadcast

```
  Client A        Client B         Server         Database
     │               │                │                │
     │  POST /save   │                │                │
     ├──────────────────────────────>│                │
     │               │                │  INSERT        │
     │               │                ├───────────────>│
     │               │                │<───────────────┤
     │               │                │  success       │
     │  {ok:true}    │                │                │
     │<───────────────────────────────┤                │
     │               │                │                │
     │               │                │  Broadcast     │
     │  WS: candle_completed          │                │
     │<───────────────────────────────┤                │
     │               │                │                │
     │               │  WS: candle_completed          │
     │               │<───────────────┤                │
     │               │                │                │
     │  Update chart │  Update chart  │                │
     │               │                │                │
```

## Deterministic Generation Flow

```
Input: symbol, timeframe, version, index, prevClose
                      ▼
┌─────────────────────────────────────────────────┐
│  Build Seed String                              │
│  seed = "BTCUSD|1|v1|2025-11-09|candle|42"      │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Hash to 4 uint32 seeds (xmur3)                 │
│  s1, s2, s3, s4 = hash(seed)                    │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Initialize PRNG (sfc32)                        │
│  rng = sfc32(s1, s2, s3, s4)                    │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Generate 2 uniform randoms                     │
│  u1 = rng()                                     │
│  u2 = rng()                                     │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Box-Muller Transform → Gaussian                │
│  z = sqrt(-2*log(u1)) * cos(2*PI*u2)            │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Calculate Price Movement                       │
│  pctMove = z * volatility * sqrt(timeframe)     │
│  close = prevClose * (1 + pctMove)              │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Build OHLC                                     │
│  open = prevClose                               │
│  high = max(o,c) * (1 + highFactor)             │
│  low = min(o,c) * (1 - lowFactor)               │
│  [highFactor, lowFactor from separate seed]     │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Round to Decimals                              │
│  round(price, priceDecimals)                    │
└──────────────────────┬──────────────────────────┘
                       ▼
              Output: Candle OHLC
```

## Partial Candle Interpolation

```
Input: Current server time, candle start time
                      ▼
┌─────────────────────────────────────────────────┐
│  Generate Target (Completed) Candle             │
│  target = generateDeterministicCandle(...)      │
│  This is what the candle WILL BE when done      │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Calculate Time Progress                        │
│  elapsed = serverTime - candleStartTime         │
│  f = clamp(elapsed / timeframeDuration, 0, 1)   │
│  f = 0.0 → start, f = 1.0 → complete            │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Interpolate Current Close                      │
│  curClose = open + (target.close - open) * f    │
└──────────────────────┬──────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────┐
│  Scale High/Low by Progress                     │
│  curHigh = max(o, curClose) * (1 + high_f * f)  │
│  curLow = min(o, curClose) * (1 - low_f * f)    │
└──────────────────────┬──────────────────────────┘
                       ▼
           Output: Partial Candle
           (marked with isPartial: true)
```

## No Rollback Guarantee

```
Timeline: Client session with refresh

t=0     Load page
        ├─ Fetch 200 candles from server
        ├─ Display on chart
        └─ Start partial candle updates (every 1s)

t=60s   1 minute passes
        ├─ Partial candle reaches f=1.0 (100% complete)
        ├─ Client saves candle via POST /save_candle
        └─ Server persists to database ✓

t=120s  2 minutes pass
        └─ Another candle saved ✓

t=180s  3 minutes pass
        └─ Another candle saved ✓

t=200s  **USER REFRESHES PAGE (F5)**
        ├─ Page reloads
        ├─ GET /api/ohlc fetches candles
        ├─ Server returns:
        │   ├─ All 200 original candles
        │   ├─ The 3 saved candles ✓
        │   └─ Current partial candle
        └─ Chart renders:
            ├─ NO JUMP BACKWARD ✓
            ├─ All saved candles appear ✓
            └─ Partial continues from correct position ✓

Result: ZERO ROLLBACK — seamless continuation
```

## Database Idempotency Flow

```
Scenario: Two clients save same candle concurrently

Client A                 Client B                Database
   │                        │                        │
   │  POST /save            │                        │
   ├───────────────────────────────────────────────>│
   │                        │                        │
   │                        │  POST /save            │
   │                        ├───────────────────────>│
   │                        │                        │
   │                        │   ┌──────────────────┐ │
   │                        │   │ UNIQUE constraint│ │
   │                        │   │ (s, tf, v, time) │ │
   │                        │   └──────────────────┘ │
   │                        │                        │
   │                        │   Client A: INSERT ✓   │
   │<───────────────────────────────────────────────┤
   │  {ok:true, inserted:true}                      │
   │                        │                        │
   │                        │   Client B: CONFLICT   │
   │                        │   → INSERT IGNORED     │
   │                        │<───────────────────────┤
   │                        │  {ok:true, inserted:false}
   │                        │                        │
   
Result: Only ONE row in database ✓
        Both clients get success response ✓
        No duplicates, no errors ✓
```

## Time Precision & Conversion

```
Internal Storage (Database & API):
┌─────────────────────────────────────┐
│  UTC Milliseconds (BIGINT)          │
│  Example: 1731043200000             │
│  = Nov 8, 2024 10:00:00.000 UTC     │
└─────────────────────────────────────┘
                 │
                 ▼
Conversion for Lightweight Charts:
┌─────────────────────────────────────┐
│  UTC Seconds (Integer)              │
│  time = Math.floor(ms / 1000)       │
│  Example: 1731043200                │
└─────────────────────────────────────┘
                 │
                 ▼
Rendered on Chart:
┌─────────────────────────────────────┐
│  Human-readable timestamp           │
│  "Nov 8, 10:00"                     │
└─────────────────────────────────────┘

KEY: Always use UTC milliseconds internally
     Convert to seconds ONLY when passing to chart
```

## Seed → Candles Mapping

```
Same Seed = Same Candles (ALWAYS)

Seed: "BTCUSD|1|v1|2025-11-09"
              ▼
┌──────────┬──────────┬──────────┬──────────┐
│ Index 0  │ Index 1  │ Index 2  │ Index 3  │
├──────────┼──────────┼──────────┼──────────┤
│ O: 42000 │ O: 42020 │ O: 42010 │ O: 42030 │
│ H: 42050 │ H: 42045 │ H: 42035 │ H: 42060 │
│ L: 41990 │ L: 42000 │ L: 41995 │ L: 42020 │
│ C: 42020 │ C: 42010 │ C: 42030 │ C: 42045 │
└──────────┴──────────┴──────────┴──────────┘

SAME seed run again → IDENTICAL candles ✓

Different seed: "BTCUSD|1|v2|2025-11-09"
              ▼
┌──────────┬──────────┬──────────┬──────────┐
│ Index 0  │ Index 1  │ Index 2  │ Index 3  │
├──────────┼──────────┼──────────┼──────────┤
│ O: 42000 │ O: 42015 │ O: 42008 │ O: 42025 │
│ H: 42045 │ H: 42040 │ H: 42030 │ H: 42055 │
│ L: 41985 │ L: 42005 │ L: 41990 │ L: 42015 │
│ C: 42015 │ C: 42008 │ C: 42025 │ C: 42040 │
└──────────┴──────────┴──────────┴──────────┘

Different candles ✓ (version changed v1→v2)
```

## Server vs Client Parity

```
Server (Node.js)                Client (Browser)
┌──────────────────┐            ┌──────────────────┐
│ shared/rng.js    │  ═══════   │ public/rng.js    │
│ • xmur3()        │  IDENTICAL │ • xmur3()        │
│ • sfc32()        │  CODE      │ • sfc32()        │
│ • gaussian()     │            │ • gaussian()     │
└──────────────────┘            └──────────────────┘

┌──────────────────┐            ┌──────────────────┐
│shared/generator.js═══════════ │public/generator.js
│ • generate...()  │  IDENTICAL │ • generate...()  │
│ • formulas       │  LOGIC     │ • formulas       │
└──────────────────┘            └──────────────────┘

WHY? To guarantee identical candles on both sides
     Server = authoritative, client = fallback/validation
```

## API Endpoint Quick Reference

```
┌────────────────────────────────────────────────────────┐
│  GET /api/ohlc                                         │
│  ├─ Params: symbol, timeframeMinutes, start, end, ver │
│  ├─ Returns: { candles: [...], partial: {...} }       │
│  └─ Use: Initial chart load + refresh                 │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  POST /api/save_candle                                 │
│  ├─ Body: { meta: {...}, candle: {...} }              │
│  ├─ Returns: { ok: true, inserted: true/false }       │
│  └─ Use: Persist completed candle                     │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  GET /api/last_saved                                   │
│  ├─ Params: symbol, timeframeMinutes, version          │
│  ├─ Returns: { lastCandle: {...} }                    │
│  └─ Use: Check what's been persisted                  │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│  WS /ws                                                │
│  ├─ Receive: { type: "candle_completed", ... }        │
│  └─ Use: Real-time updates                            │
└────────────────────────────────────────────────────────┘
```

## Testing Pyramid

```
                    ┌─────────┐
                    │ Manual  │
                    │  Test   │ ← No-rollback verification
                    │ (1 test)│
                    └─────────┘
                 ┌────────────────┐
                 │  Integration   │
                 │     Tests      │ ← API endpoints, WebSocket
                 │   (future)     │
                 └────────────────┘
           ┌──────────────────────────┐
           │      Unit Tests          │
           │  • Determinism           │
           │  • Idempotency          │ ← 8 automated tests
           │  • OHLC validation      │
           │  • Database ops         │
           └──────────────────────────┘
```

---

**Navigation:**
- [← Back to Index](INDEX.md)
- [Documentation Index →](INDEX.md)

**Last Updated:** November 9, 2025
